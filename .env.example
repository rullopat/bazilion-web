# Copy this file to `.env` and fill in your values.
# `.env` is gitignored. The `deploy` npm script loads it via dotenv-cli
# and exports the values to `wrangler deploy`.

# Your Cloudflare account ID. Find it in the Cloudflare dashboard
# sidebar, or by running `npx wrangler whoami`.
CLOUDFLARE_ACCOUNT_ID=
